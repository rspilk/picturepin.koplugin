local _ = require("gettext")
return {
    fullname = _("PicturePin"),
    description = _([[Pin an image from the book (e.g. a map) to a small persistent icon that reopens it without losing your reading position.]]),
}
